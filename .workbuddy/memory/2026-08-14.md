# 2026-08-14

## 数据下载体系重构（注册表 + profile 策略）

基于此前 database 审计结论，把单一 GB1 硬编码下载脚本重构为通用体系：

- `configs/data/datasets.yaml`：13 个数据集注册表（FLIP GB1/AAV/AAV splits、ProteinGym reference/substitutions-mvp/full/cv-folds/indels/structures/msa-mvp/msa-full、FLIP2 priority/full）。固定版本：FLIP commit `62cace8`、ProteinGym v1.3 + commit `144fe22`、FLIP2 2025。URL 均经 GitHub API/官方 README 核实。
- `configs/data/profiles/{smoke,core,evidence,extended,full}.yaml`：core = GB1+AAV+MVP assays；evidence +结构/MSA；extended +FLIP2；full 全量（~8GB）。
- `configs/data/proteingym_mvp_assays.txt`：7 个 MVP assay，已在 DMS_substitutions.csv 中验证全部存在。
- `src/fitness_agents/data/download.py`：通用引擎——镜像回退、Range 断点续传、.partial 原子重命名、sha256（pin 或 TOFU 记录）、zip 成员白名单 + 路径穿越防护、每数据集 download_manifest.json、--force/--offline/--verify-only。
- 入口：`scripts/data/download_profile.py`（主）、`download_flip.sh`、`download_flip2.py`、`download_proteingym.py`、`download_msas.py`、`download_structures.py`、`validate_downloads.py`；`download_flip_gb1.sh` 改为兼容瘦包装。
- 已 pin 的 sha256：GB1 archive、GB1 README、5LDE_1.fasta、ProteinGym 两个 reference CSV。其余文件为 TOFU，首次下载后需把 manifest 里的 digest pin 回注册表。
- 自测：`tests/_manual_download_engine_test.py` 8 项合成端到端测试全过；GB1（149,361 行）和 proteingym_reference（217 assays）真实下载校验通过。
- 注意：raw.githubusercontent.com 偶发连接重置，重试可恢复；大文件（substitutions 1GB、MSA 5.2GB）尚未实际下载。

## 后续待办
- 数据适配层（DatasetAdapter: FlipGB1/FlipAAV/Flip2/ProteinGym）统一 schema 尚未实现，gb1.py 仍硬编码四位点。
- prepare_flip.py / prepare_proteingym.py / build_campaign_splits.py 待写。

## 配置回滚事件（21:03）
- 发现 `proteingym_mvp_assays.txt` 和 `datasets.yaml` 中的两个 SPIKE_SARS2_Starr_2020 assay（binding/expression）曾被移除，并留有"viral assays out of scope"注释。按用户要求已恢复：assay 清单 7 个完整、`expected_min_rows` 重新包含两个 SPIKE 条目，并更新注释说明 Spike 用于多目标约束验证。Spike binding+expression 是项目多目标/证据消融的关键数据，不应移除。
